version: "3"
services:
    web-balea:
        image: app
        container_name: app
        ports:
            - 8060:80
        build:
            context: ..
            dockerfile: build/Dockerfile
